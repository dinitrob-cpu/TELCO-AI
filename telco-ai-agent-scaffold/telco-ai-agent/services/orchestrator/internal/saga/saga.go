// Package saga implements cross-subagent coordination, e.g. triggering
// Fault Corr with a bounded window when Anomaly SubAgent raises a
// score above threshold.
package saga

// TODO: define saga steps once Anomaly + FaultCorr gRPC clients exist.
